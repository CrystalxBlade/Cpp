##  not really a container but Stores two values together, more a utility. Used heavily with map and unordered_map.

```
(first, second)

(1, "Blade")
(10, 99)
```

- 🧠 Think: A tiny backpack with exactly 2 pockets 