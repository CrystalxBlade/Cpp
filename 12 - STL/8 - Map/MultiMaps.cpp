#include <iostream>
#include <map>

int main()
{

    /*

    std::multimap<std::string, int> m;

    m.emplace("tv", 100);
    m.emplace("tv", 100);
    m.emplace("tv", 150);
    m.emplace("tv", 50); 

    m.erase("tv");                      // delete all the keys with same name

    for(auto p : m)
    {
        std::cout << p.first << " " << p.second << '\n';
    }

    */

    return 0;
}