## Enum in C++

This example demonstrates how to use an enum to represent a fixed set
of related values (days of the week).

Why enum?
- Improves code readability
- Avoids magic numbers
- Makes logic clearer and safer

Concepts used:
- enum
- switch statement
