## virtual function

### A virtual function is a member function declared with virtual in a base class that allows a derived class to override it, and ensures the correct function is called at runtime.

- Dynamic in nature

- Defined by the keyword "virtual" inside base class and are always declared wiht a base class and
  overriden in child class

- It is called during runtime   