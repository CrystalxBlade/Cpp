#include <iostream>
#include <vector>

int main()
{
          
    /*                // size

    std::vector<char> vec = {'a', 'b', 'c', 'd', 'e'};
    vec.size();  
    
                      // push back
    std::vector<char> vec = {'a', 'b', 'c', 'd', 'e'};
    vec.push_back('z');

    for(char c : vec)
    {
        std::cout << c << " ";
    }

                      // pop back
    std::vector<char> vec = {'a', 'b', 'c', 'd', 'e'};
    vec.pop_back();

    for(char c : vec)
    {
        std::cout << c << " ";
    }
        
                     // front & back
    std::vector<char> vec = {'a', 'b', 'c', 'd', 'e'};

    std::cout << "front : " << vec.front() << '\n';
    std::cout << "back : " << vec.back();

    */
    return 0;
}